# Client-side (safe to expose) Supabase values
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
# Site URL used by auth callbacks (Render URL or local)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OPTIONAL server-side key (only for server scripts or API routes that need elevated privileges).
# DO NOT expose this to the client.
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
